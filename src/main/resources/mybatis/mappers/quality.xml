<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.quality">
	<!-- 검사관리 리스트 -->
	<!-- 조인해서 필요한 것: 품목명, LOT 번호, 검사자 -->
	<select id="_select_Inspection"
		resultType="kr.or.saeroi.dto.InspectionDTO">
		SELECT
		i.doc_no,
		i.insp_date,
		it.item_name,
		w.product_lot,
		e.ename,
		i.result

		FROM inspection i, production p,work_order w,
		production_plan
		pr, item it, emp e
		where i.prod_id = p.prod_id
		and p.order_id =
		w.order_id
		and w.prod_plan_id = pr.prod_plan_id
		and pr.item_id =
		it.item_id
		and i.emp_id = e.emp_id

		<!-- 날짜 조건 -->
		<if test="startDate != null and startDate != ''">
			AND i.insp_date &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
		</if>
		<if test="endDate != null and endDate != ''">
			AND i.insp_date &lt;= TO_DATE(#{endDate}, 'YYYY-MM-DD')
		</if>
		<!-- 검색어 조건 -->
		<if
			test="searchType != null and searchType != '' and keyword != null and keyword != ''">
			<choose>
				<when test="searchType == 'insp_id'">
					AND i.doc_no LIKE '%' || #{keyword} || '%'
				</when>
				<when test="searchType == 'itemName'">
					AND it.item_name LIKE '%' || #{keyword} || '%'
				</when>
				<when test="searchType == 'productLot'">
					AND w.product_lot LIKE '%' || #{keyword} || '%'
				</when>
				<when test="searchType == 'ename'">
					AND e.ename LIKE '%' || #{keyword} || '%'
				</when>
				<when test="searchType == 'result'">
					AND i.result LIKE '%' || #{keyword} || '%'
				</when>
			</choose>
		</if>

	</select>

	<!-- 구분 옵션 -->
	<select id="_select_Inspection_option" resultType="string">
		SELECT option_value
		FROM (
		SELECT option_value,
		ROW_NUMBER() OVER (ORDER
		BY option_value) AS rn
		FROM (
		SELECT DISTINCT
		<choose>
			<when test="searchType == 'item_id'">
				it.item_id
			</when>
			<when test="searchType == 'itemName'">
				it.item_name
			</when>
			<when test="searchType == 'productLot'">
				w.product_lot
			</when>
			<when test="searchType == 'ename'">
				e.ename
			</when>
			<when test="searchType == 'result'">
				i.result
			</when>
			<when test="searchType == 'remark'">
				i.remark
			</when>
			<otherwise>
				NULL
			</otherwise>
		</choose>
		AS option_value
		FROM inspection i,
		production p,
		work_order w,
		production_plan pr,
		item it,
		emp e
		WHERE i.prod_id = p.prod_id
		AND
		p.order_id = w.order_id
		AND w.prod_plan_id = pr.prod_plan_id
		AND
		pr.item_id = it.item_id
		AND i.emp_id = e.emp_id

		<if test="startDate != null and startDate != ''">
			AND i.insp_date &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
		</if>

		<if test="endDate != null and endDate != ''">
			AND i.insp_date &lt;= TO_DATE(#{endDate}, 'YYYY-MM-DD')
		</if>
		)
		WHERE option_value IS NOT NULL
		)
		WHERE rn BETWEEN ((#{optionPage} - 1)
		* #{optionSize} + 1)
		AND (#{optionPage} * #{optionSize})
	</select>

	<!-- 등록 쿼리 -->
	<!-- insert의 경우 parameterType 사용 -->
	<insert id="_insert_Inspection"
		parameterType="kr.or.saeroi.dto.InspectionDTO">

		INSERT INTO inspection (
		insp_id,
		emp_id,
		prod_id,
		insp_type,
		insp_date,
		insp_status,
		result,
		inspection_qty,
		good_qty,
		remark,
		created_date,
		updated_date
		)
		VALUES (
		(
		
		)
	</insert>

</mapper>
