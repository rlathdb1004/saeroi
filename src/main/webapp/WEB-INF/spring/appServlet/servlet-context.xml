<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing 
		infrastructure -->

	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving 
		up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />
	<!-- 사진 저장 경로 추가 -->
	<resources mapping="/upload/defect/**"
		location="file:///C:/team3/upload/defect/" />
	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/views directory -->

	<!-- 일반 JSP 화면을 찾는 설정 -->
	<beans:bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">

		<!-- Controller에서 return한 이름 앞에 붙는 JSP 기본 경로 -->
		<beans:property name="prefix" value="/WEB-INF/views/" />

		<!-- Controller에서 return한 이름 뒤에 붙는 JSP 확장자 -->
		<beans:property name="suffix" value=".jsp" />

		<!-- Tiles를 먼저 찾고, 일반 JSP는 두 번째로 찾게 한다. -->
		<beans:property name="order" value="2" />

	</beans:bean>

	<!-- Tiles 설정 파일을 읽어오는 설정 -->
	<beans:bean id="tilesConfigurer"
		class="org.springframework.web.servlet.view.tiles3.TilesConfigurer">

		<!-- Tiles 설정 파일 위치 -->
		<beans:property name="definitions">
			<beans:list>
				<!-- src/main/resources/tiles 폴더 안의 xml 파일을 읽는다. -->
				<beans:value>classpath:tiles/*.xml</beans:value>
			</beans:list>
		</beans:property>

	</beans:bean>

	<!-- .tiles로 끝나는 화면을 Tiles 방식으로 처리하는 설정 -->
	<beans:bean id="tilesViewResolver"
		class="org.springframework.web.servlet.view.UrlBasedViewResolver">

		<!-- Tiles 화면으로 처리하겠다는 설정 -->
		<beans:property name="viewClass"
			value="org.springframework.web.servlet.view.tiles3.TilesView" />

		<!-- 일반 JSP보다 Tiles를 먼저 찾게 한다. -->
		<beans:property name="order" value="1" />

	</beans:bean>

	<!-- aiChatbot사용 위해서 추가 -->
	<beans:bean id="restTemplate"
		class="org.springframework.web.client.RestTemplate">
		<beans:constructor-arg>
			<beans:bean
				class="org.springframework.http.client.HttpComponentsClientHttpRequestFactory">
				<beans:property name="connectTimeout" value="5000" />
				<beans:property name="readTimeout" value="60000" />
			</beans:bean>
		</beans:constructor-arg>
	</beans:bean>


	<!-- ai챗봇이 api키 제대로 읽을수 있게 -->
	<context:property-placeholder
		location="classpath:application.properties" />

	<context:component-scan
		base-package="kr.or.saeroi" />

	<!-- 인터셉터 등록 -->
	<mvc:interceptors>
		<!--자동로그인 -->
		<mvc:interceptor>
			<mvc:mapping path="/**" />
			<beans:bean
				class="kr.or.saeroi.loginConfig.AutoLoginInterceptor" />
		</mvc:interceptor>

		<!-- 권한 체크 인터셉터 -->
		<mvc:interceptor>
			<mvc:mapping path="/**" />

			<mvc:exclude-mapping path="/resources/**" />
	<!-- 사진 등록 경로 추가 -->
			<mvc:exclude-mapping path="/upload/defect/**" />
			<mvc:exclude-mapping path="/login" />
			<mvc:exclude-mapping path="/logout" />

			<beans:bean
				class="kr.or.saeroi.loginConfig.RoleInterceptor" />
		</mvc:interceptor>
	</mvc:interceptors>


	<!-- 파일 업로드 설정 -->
	<beans:bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">

		<!-- 업로드 요청 인코딩 -->
		<beans:property name="defaultEncoding" value="UTF-8" />

		<!-- 최대 업로드 용량: 10MB -->
		<beans:property name="maxUploadSize" value="10485760" />

		<!-- 메모리에 올릴 최대 크기: 1MB -->
		<beans:property name="maxInMemorySize" value="1048576" />

	</beans:bean>


</beans:beans>